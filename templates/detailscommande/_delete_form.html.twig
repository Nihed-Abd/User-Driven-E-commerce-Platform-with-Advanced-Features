<form method="post" action="{{ path('app_detailscommande_delete', {'id': detailscommande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detailscommande.id) }}">
    <button class="btn">Delete</button>
</form>
