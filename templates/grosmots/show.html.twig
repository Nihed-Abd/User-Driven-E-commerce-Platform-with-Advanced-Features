{% extends 'base.html.twig' %}

{% block title %}Grosmots{% endblock %}

{% block body %}
    <h1>Grosmots</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdGm</th>
                <td>{{ grosmot.idGm }}</td>
            </tr>
            <tr>
                <th>Grosmots</th>
                <td>{{ grosmot.grosmots }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_grosmots_index') }}">back to list</a>

    <a href="{{ path('app_grosmots_edit', {'idGm': grosmot.idGm}) }}">edit</a>

    {{ include('grosmots/_delete_form.html.twig') }}
{% endblock %}
