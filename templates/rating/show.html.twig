{% extends 'base.html.twig' %}

{% block title %}Rating{% endblock %}

{% block body %}
    <h1>Rating</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>RatingId</th>
                <td>{{ rating.ratingId }}</td>
            </tr>
            <tr>
                <th>RatingValue</th>
                <td>{{ rating.ratingValue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rating_index') }}">back to list</a>

    <a href="{{ path('app_rating_edit', {'ratingId': rating.ratingId}) }}">edit</a>

    {{ include('rating/_delete_form.html.twig') }}
{% endblock %}
