<form method="post" action="{{ path('app_commentaire_delete', {'idCom': commentaire.idCom}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.idCom) }}">
    <button class="btn" style="background-color: red; color: white;">Supprimer🗑</button>
</form>
