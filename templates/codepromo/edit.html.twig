{% extends 'base.html.twig' %}

{% block title %}Edit Codepromo{% endblock %}

{% block body %}
    <h1>Edit Codepromo</h1>

    {{ include('codepromo/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_codepromo_index') }}">back to list</a>

    {{ include('codepromo/_delete_form.html.twig') }}
{% endblock %}
