<form method="post" action="{{ path('app_codepromo_delete', {'idcode': codepromo.idcode}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ codepromo.idcode) }}">
    <button class="btn">Delete</button>
</form>
