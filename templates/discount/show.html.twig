{% extends 'base.html.twig' %}

{% block title %}Discount{% endblock %}

{% block body %}
    <h1>Discount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idd</th>
                <td>{{ discount.idd }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ discount.userid }}</td>
            </tr>
            <tr>
                <th>Codepromoid</th>
                <td>{{ discount.codepromoid }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ discount.date ? discount.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_discount_index') }}">back to list</a>

    <a href="{{ path('app_discount_edit', {'idd': discount.idd}) }}">edit</a>

    {{ include('discount/_delete_form.html.twig') }}
{% endblock %}
