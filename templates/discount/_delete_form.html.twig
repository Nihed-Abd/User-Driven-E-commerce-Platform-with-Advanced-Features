<form method="post" action="{{ path('app_discount_delete', {'idd': discount.idd}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discount.idd) }}">
    <button class="btn">Delete</button>
</form>
