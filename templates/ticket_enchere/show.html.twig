{% extends 'base.html.twig' %}

{% block title %}TicketEnchere{% endblock %}

{% block body %}
    <h1>TicketEnchere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>TicketId</th>
                <td>{{ ticket_enchere.ticketId }}</td>
            </tr>
            <tr>
                <th>EnchereId</th>
                <td>{{ ticket_enchere.enchereId }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ ticket_enchere.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ticket_enchere_index') }}">back to list</a>

    <a href="{{ path('app_ticket_enchere_edit', {'ticketId': ticket_enchere.ticketId}) }}">edit</a>

    {{ include('ticket_enchere/_delete_form.html.twig') }}
{% endblock %}
